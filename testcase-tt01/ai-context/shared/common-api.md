# 共享接口

> 多端调用的同一套接口。若某接口在多个端定义完全一致，应迁移至此处，各端 `api.md` 以引用方式标注。

## 接口列表

### {接口名} (POST /api/...)

- **描述**：{简要说明}
- **调用端**：{pc-client / web-admin / ...}
- **请求参数**：

  | 参数 | 类型 | 必填 | 说明 |
  | ---- | ---- | ---- | ---- |
  | {参数} | {string} | 是 | {说明} |

- **成功响应示例**：

  ```json
  { "code": 200, "data": {} }
  ```

- **错误码**：

  | 错误码 | 含义 |
  | ------ | ---- |
  | 400 | 参数缺失 |

> 待补充：根据真实接口补充共享接口。
