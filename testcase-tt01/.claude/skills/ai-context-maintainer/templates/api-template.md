# {模块名} 接口定义

> 变更记录：
> [YYYY-MM-DD] 初始创建

## 接口列表

### {接口名} (POST /api/...)

- **描述**：{简要说明接口功能}
- **鉴权**：{Token / Session / 签名}

- **请求参数**：
  | 参数 | 类型 | 必填 | 说明 |
  |------|------|------|------|
  | {参数} | {string} | 是 | {说明} |

- **成功响应示例**：
  ```json
  {
    "code": 200,
    "data": {}
  }
  ```

- **错误码**：
  | 错误码 | 含义 |
  |--------|------|
  | 400 | 参数缺失 |